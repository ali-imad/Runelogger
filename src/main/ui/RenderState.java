package ui;

/*
    A enumeration representing what should be displayed by the GameWindow during some rendering tick. Should be updated
    with new state transitions
 */
public enum RenderState {
    MENU, BOSS_VIEW, RECENT_VIEW, NEW_ENTRY, EXIT, REMOVE
}
