package model.actor;

public enum Stats {
    HP,  // life force
    MP,  // special attack resource
    ATK,  // determines hit chance and max hit of normal attacks
    DEF,  // determines dmg reduction and HP regen per turn
    WIS,  // determines special damage and regen per turn
}
